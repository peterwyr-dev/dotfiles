# Herdr Backslash Split Design

Replace Herdr's side-by-side split binding from `prefix+|` to `prefix+\`. Keep `prefix+minus` for stacked splits and preserve all unrelated settings. Validate TOML escaping and Herdr key syntax with Herdr 0.7.5 before installation.
