# tmux Backslash Split Design

Replace the side-by-side split binding from `prefix+|` to `prefix+\`. Preserve the inherited working directory, `prefix+-` stacked split, and every unrelated option and binding. Verify the effective tmux prefix table in an isolated server.
