# Kitty Top Padding Design

Change Kitty's window padding from a uniform 15 points to CSS-style `0 15 15 15`, removing only the top padding. Preserve right, bottom, and left padding, the top tab bar, margins, theme, fonts, and all keybindings. Validate the parsed edges with the installed Kitty 0.48.2 runtime.
