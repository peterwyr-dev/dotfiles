# tmux Top Status Design

Add `set -g status-position top` to the personal configuration section. Preserve all Catppuccin status content, colors, separators, and plugin configuration. Verify the effective value in an isolated tmux server.
