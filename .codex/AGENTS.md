# Global Codex Defaults

- Use normal clear prose by default.
- Keep the user's language.
- Preserve code, command names, API names, exact error strings, and commit messages verbatim.
- Use `caveman` only when explicitly requested with `$caveman`, `/caveman`, or equivalent wording.
